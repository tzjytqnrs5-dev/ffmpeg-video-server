[phases.setup]
# CRITICAL FIX: Removed 'libfdk_aac' as it caused the build failure.
# We keep the essential packages: 'nodejs', 'ffmpeg', and 'libx264'.
nixPkgs = ['nodejs', 'ffmpeg', 'libx264'] 

[phases.install]
cmds = ['npm install']

[start]
cmd = 'npm start'
